{
  "name": "the-bakers-guild",
  "private": true,

  "workspaces": {
    "packages": ["client", "server"]
  },

  "scripts": {
    "build": "npm run build -w client",

    "dev": "npm run dev -w server & npm run dev -w client"
  },

  "dependencies": {
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.0"
  },

  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
